# Python
__pycache__/
*.py[cod]
*.so
.venv/

# Environment
.env
.envrc

# TLS certs written by `tailscale cert` during deploy — never commit
/certs/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
coverage.xml

# macOS
.DS_Store

# Runtime artifacts (all three are bind-mounted volumes in Docker)
/assets/video/
/assets/meta/
/assets/logs/

# YOLO model weights (downloaded at Docker build time, not committed)
/models/

# Legacy
/data/
